// 导出所有类型和函数
export type {
  BaseToken,
  CSSVariables,
  ComponentToken,
  GenerateStyle,
  UseStyleResult,
} from './useStyle';

export { createStyleRegister } from './useStyle';

// 导出 hooks
export { useCSSVariables } from './hooks';
